server:
  port: 8081
spring:
  application:
    name: rental-service
  datasource:
    password: sa
    url: jdbc:h2:mem:testDB
    username: sa
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
